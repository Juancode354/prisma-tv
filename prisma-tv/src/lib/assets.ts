// Caminhos centrais dos assets visuais (servidos a partir de /public).
// Use sempre estas constantes em vez de digitar o caminho na mão.

export const BRAND = {
  logo: "/brand/prisma-logo.png",       // logo oficial completa (transparente)
  mark: "/brand/prisma-mark.png",       // prisma isolado (símbolo / ícone)
  wordmark: "/brand/prisma-wordmark.png" // texto "REDE PRISMA" (transparente)
} as const;

export const REFERENCE = {
  ficha: "/reference/ficha.jpeg",
  npcAngelo: "/reference/Angelo.jpeg"
} as const;
